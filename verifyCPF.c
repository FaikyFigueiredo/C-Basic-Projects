#include <stdio.h>

int sumBiggers(nums1[3], nums2[3], nums3[3]){

}
int main(){
    char CPF[14] , firstNums[3] , secondNums[3], thirdNums[3], result;
    printf("Put your CPF (brazzilian code to know who is the person) and I will verify if it's a valid CPF");
    scanf("%s" , CPF);
    firstNums[0] = CPF[0];
    firstNums[1] = CPF[1];
    firstNums[2] = CPF[2];
    secondNums[0] = CPF[4];
    secondNums[1] = CPF[5];
    secondNums[2] = CPF[6];
    thirdNums[0] = CPF[8];
    thirdNums[1] = CPF[9];
    thirdNums[2] = CPF[10];
    result = CPF[12];
    return 0;
}